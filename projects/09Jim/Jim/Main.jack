class Main {

    function void main() {
        var boolean menu;
        var startupMenu startup;    
        var char key;
        
        while(~menu) {
            do startupMenu.new(); //called constructor using diff method
            let key = Keyboard.readChar();
            if(key = 2) {
                do startup.help();
            } 


        }
        
        return;
    }
}